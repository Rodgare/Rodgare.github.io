<?php if (isset($flash)): ?>
  <?php foreach ($flash as $key => $messages): ?>
    <?php if ($key === 'success'): ?>
      <?php foreach ($messages as $message): ?>
        <div style="color:green;">
          <h3><?= $message ?></h3>
        </div>
      <?php endforeach ?>
    <?php else: ?>
      <?php foreach ($messages as $message): ?>
        <div style="color:red;">
          <h3><?= $message ?></h3>
        </div>
      <?php endforeach ?>
    <?php endif ?>
  <?php endforeach ?>
<?php endif ?>
<form action="/" method="post">
  <label>Email ('user@email.com')
    <input type="email" name="user[email]" value="<?= htmlspecialchars($user['email']) ?>">
  </label>
  <input type="submit" value="Вхід">
</form>